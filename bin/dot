#!/usr/bin/env python3
"""Launcher: makes the ``dot`` package importable from anywhere, then runs the CLI."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.realpath(__file__))))

from dot.cli import run  # noqa: E402

run()
